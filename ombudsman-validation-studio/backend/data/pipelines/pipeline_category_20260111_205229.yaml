metadata:
  name: category_workload_validation
  description: Workload-based validations for category
  generated_from: workload_wl_20260111_205155_126fe333
  project_id: default_project
  created_at: '2026-01-11T20:52:29.236560'
  validation_count: 1
  active: true
source:
  type: sqlserver
  database: ${SQL_DATABASE}
  schema: ${SQL_SCHEMA}
  table: category
target:
  type: snowflake
  database: ${SNOWFLAKE_DATABASE}
  schema: ${SNOWFLAKE_SCHEMA}
  table: CATEGORY
steps:
- name: validation_1_validate_cardinality
  type: cardinality
  description: Column used in GROUP BY of 1 queries (likely categorical/dimension
    key)
  confidence: 59.0
  enabled: true
  validator: cardinality_check
  config:
    columns:
    - category
    check_type: unique_count
    tolerance: 0.02
