metadata:
  name: dim_product_workload_validation
  description: Workload-based validations for dim_product
  generated_from: workload_wl_20260111_205155_126fe333
  project_id: default_project
  created_at: '2026-01-11T21:38:47.252565'
  validation_count: 1
  active: true
source:
  type: sqlserver
  database: ${SQL_DATABASE}
  schema: DIM
  table: dim_product
target:
  type: snowflake
  database: ${SNOWFLAKE_DATABASE}
  schema: DIM
  table: DIM_PRODUCT
steps:
- name: validation_1_workload_query
  type: comparative
  description: Actual workload query (executed 650 times)
  confidence: 95.0
  enabled: true
  validator: custom_sql
  config:
    sql_server_query: SELECT category, COUNT(*) FROM DIM.dim_product GROUP BY category
    snowflake_query: SELECT category, COUNT(*) FROM DIM.DIM_PRODUCT GROUP BY category
    compare_mode: result_set
    tolerance: 0.01
    ignore_column_order: true
    ignore_row_order: false
  metadata:
    query_id: 8
    validation_type: workload_query
    total_executions: 0
    avg_duration_ms: 56.8
